# Improved Skill Output — Input 03

**Input:** "I just finished generating my case study for the outflow-ai project. I want to make it more visual so it stands out in my portfolio — can you help me figure out what charts or diagrams I should add, and where they should go?"
**Skill state:** Improved (iter-1)

---

## What Happened

Description now contains "make it more visual" and "what charts should I add" — these two trigger phrases are explicit matches for the NL input. The skill fired from description-level routing (not CLAUDE.md system context dependency). Step 0 ran, detected the outflow-ai output already has a VISUAL PLAN section, and surfaced the existing plan per the "already applied" instruction.

## Skill Output

Step 0 complete. The output file `/Users/mrunalsurve/Documents/pm-case-study-builder/outputs/outflow-ai-v7-2026-05-22.md` already contains a `## VISUAL PLAN` section.

The visual layer is already applied to this file. Here's the current plan:

| ID | Section | Visual Type | Placement |
|----|---------|-------------|-----------|
| v1 | Problem | Before-state workflow diagram | After the before-state paragraph — shows pre-OutFlow coordination failure with dollar values per failure mode ($200-400 return window, $50-150 refund, $300-600 subscription) |
| v2 | Strategy | Decision matrix / options comparison | After the decision table — model selection comparison: LLM vs fine-tuned small model vs regex-only |
| v3 | Solution | Process flow diagram | After the three-layer pipeline description — full pipeline from email forwarding intake through AI extraction, rule engine, confidence routing, and draft generation |
| v4 | Outcomes | Metrics improvement chart | After the stat blocks — four funnel metrics (setup completion, 7-day return, action rate, signup conversion) vs targets |
| v5 | Execution | Timeline or milestone chart | After the offline evaluation description — three-week build timeline with Week 1 eval gate highlighted |
| v6 | Users | Drop-off funnel chart | After the pull quote — four-stage funnel from landing to action, with target rates overlaid |

All 6 placeholders are already embedded in the case study text with captions written for each visual.

Say "re-run visual layer" if you want a fresh pass on this file.

---

## Improvements vs Original

- Routing: now fires from description trigger phrases ("make it more visual", "what charts should I add") — not CLAUDE.md-dependent
- Output format: now consistent structured table (matches "already applied" instruction) instead of ad-hoc markdown prose
- Step 0 explicitly named in response — deterministic behavior vs. inferred behavior in original
- Re-run offer is explicit and consistent
