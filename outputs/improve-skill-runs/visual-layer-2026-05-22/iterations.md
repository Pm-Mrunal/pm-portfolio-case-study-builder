# Iteration Log — visual-layer

---

## Iteration 1

**What changed in SKILL.md:**

1. **Description rewrite** — expanded from 217 chars (WHAT only) to ~500 chars with WHAT+WHEN in first 250, 7 explicit trigger phrases ("add visuals", "add visual placeholders", "add the visual layer", "make it more visual", "what charts should I add", "add diagrams to my case study", "insert visual placeholders"), and "Do NOT use for" boundaries with /Y pointers to /polish, /generate, /gap-detect, /build-portfolio.

2. **"When to Use" body section removed** — content moved to description (audit item #6: trigger phrases in body are invisible to the router).

3. **"## Inputs" renamed and rewritten as "## Step 0: Detect Inputs Before Running"** — added Source/Path/Extract table (Tip 4); added three explicit branching paths: no-input stop, "already applied" surface-and-offer, proceed. Fixes both the principled-decline path (now instruction-driven) and the "already applied" re-run case (now has a deterministic instruction instead of being improvised).

4. **"What This Skill Does NOT Do" updated with /Y pointers** — "Does not modify factual content → use /polish"; "Does not generate actual images → use a design tool after plan is complete".

5. **Anti-rationalization table added** (Tip 11) — 5 rows covering the most likely step-skipping shortcuts: inferring content from project name, silently adding visuals when layer already applied, skipping captions, exceeding TLDR placeholder limit, describing Step 4 instead of executing it.

6. **"Quality Checks Before Finishing" restructured as "Before Marking Complete"** — repositioned as mandatory pre-save gate (not post-save advisory); added Step 0 verification as the first checklist item; added final file section count check.

**What moved on the rubric:**

| Criterion | Before | After | Delta |
|-----------|--------|-------|-------|
| Routing quality | 9.3/20 avg | 18.0/20 avg | +8.7 |
| Output specificity | 17.3/20 avg | 18.0/20 avg | +0.7 |
| Output format consistency | 14.7/20 avg | 18.0/20 avg | +3.3 |
| Step completeness | 16.0/20 avg | 18.0/20 avg | +2.0 |
| Non-fabrication adherence | 17.3/20 avg | 18.0/20 avg | +0.7 |
| **Total** | **74.7/100** | **90.0/100** | **+15.3** |

**All inputs hit A+ (90/100). Loop ends.**
