# Input 01 — Direct Invocation (Principled Decline Test)

**Type:** Direct invocation with no case study provided

**User message:**
```
/visual-layer
```

**What this tests:**
Whether the skill correctly stops and requests the missing case study output, rather than proceeding with fabricated or generic visuals. The correct output is a principled decline that names the missing input and suggests a next step (provide the output file or run /generate first).
